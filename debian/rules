#!/usr/bin/make -f

%:
	dh $@

override_dh_auto_install:
	ifeq ($(DEB_HOST_ARCH),armhf)
		cp debian/kdeglobals.armhf debian/tmp/etc/xdg/kdeglobals
	else
		cp debian/kdeglobals debian/tmp/etc/xdg/kdeglobals
	endif
	$(dh_auto_install) --destdir=debian/tmp
